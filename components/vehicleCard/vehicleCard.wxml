<!-- 车辆卡片组件 -->
<view class="vehicle-card">
  <view class="card-content">
    <view class="image-container">
      <image class="vehicle-image" src="{{vehicle.image}}" mode="aspectFill"></image>
    </view>
    
    <view class="info-container">
      <view class="header">
        <text class="plate">{{vehicle.plate}}</text>
        <view class="status-tag {{vehicle.statusClass}}">{{vehicle.status}}</view>
      </view>
      
      <view class="model">{{vehicle.model}}</view>
      
      <view class="features">{{vehicle.features}}</view>
      
      <view class="stats-and-action">
        <view class="stats">
          <text class="stat-item">
            <text class="stat-icon film">🎬</text>
            素材 {{vehicle.materialCount}}
          </text>
          <text class="stat-item">
            <text class="stat-icon clapperboard">🎬</text>
            成片 {{vehicle.clipCount}}
          </text>
          <text class="stat-item">
            <text class="stat-icon paper">✈️</text>
            已发 {{vehicle.publishCount}}
          </text>
        </view>
        <button class="action-button {{vehicle.actionType}}">{{vehicle.actionText}}</button>
      </view>
    </view>
  </view>
</view>